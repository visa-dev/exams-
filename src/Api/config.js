// config.js

// Base URL for the API
export const API_BASE_URL = 'http://localhost:8080'; // Replace with your actual base URL

// Other global configurations can be added here if needed
export const APP_NAME = 'MyApp'; // Example: Application name
export const DEFAULT_TIMEOUT = 10000; // Example: Default timeout for API calls in milliseconds

// Example: Environment-based configuration
export const ENVIRONMENT = process.env.NODE_ENV || 'development'; // 'development' or 'production'
